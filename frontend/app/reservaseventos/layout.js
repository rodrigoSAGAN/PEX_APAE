export const metadata = {
  title: "Reservas de Eventos",
  description: "Acompanhe as reservas confirmadas dos eventos da APAE Pinhão.",
};

export default function Layout({ children }) {
  return children;
}
